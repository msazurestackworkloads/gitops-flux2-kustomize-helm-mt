# Default values
# https://github.com/bitnami/charts/blob/master/bitnami/redis/values.yaml
usePassword: false
cluster:
  enabled: false
master:
  persistence:
    enabled: false
replica:
  persistence:
    enabled: false
image:
  repository: bitnamilegacy/redis

volumePermissions:
  enabled: true
